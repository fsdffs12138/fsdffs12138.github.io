---
description: Hexo 博客作品集（portfolio）文章的写作规范与 tag 体系
alwaysApply: true
---

# 项目概况

- Hexo + Butterfly 主题的个人站，源码在 `source/`，作品集文章在 `source/_posts/portfolio/`
- 封面图统一放 `source/img/portfolio/`，B 站视频封面文件名用 `BVxxx.jpg`，其他用作品英文名
- 构建产物 `public/` 和 `db.json` 会跟着提交（仓库现状就是这样，别去动）

# 作品集文章统一结构（作品集类）

frontmatter 字段顺序：`title → date → categories → tags → cover → description → sticky`

正文章节按以下顺序出现（有则保留，无则跳过，不要硬凑）：

1. `## 视频 PV` / `## 游戏实况` / `## 在线试玩` —— B 站 iframe 或 itch.io embed
2. `## 项目简介` —— 必有
3. `## 玩法设计` / `## 核心数值` —— 有细节才写
4. `## 视听与氛围` —— 有细节才写
5. `## 我的职责` —— 分 **策划 / 程序 / 美术协作** 等条目
6. `## 获奖情况` —— 有才写，用 🏆 emoji
7. `## 项目链接` —— 统一用这个名字，**不要**用"相关链接/试玩链接"。条目用 🎮 在线试玩 / 📦 仓库 / 🎥 视频 / 📖 档案 / 💾 下载
8. `## 项目信息` —— 必有，字段顺序固定：**时间 → 主题/赛事 → 类型 → 引擎/平台 → 角色 → 团队 → 成果**
   - `引擎` 只在这里出现，不要在"项目链接"里再写"技术栈"重复

# 工作经历类文章统一结构

- `categories: 工作经历`
- 正文顺序：`## 项目预告`（如有视频）→ `## 工作信息`
- 工作信息字段顺序：**公司 → 部门 → 项目 → 角色 → 工作内容 → 时间 → 成果**

# Tag 体系（精简后的白名单）

**技术/引擎**：`Unity` / `LLM`（不要写 `C#`，Unity 隐含；不要写 `AI 对话`，用 `LLM`）

**玩法类型**：`平台跳跃`（不要 `2D 平台跳跃`）/ `ACT` / `解密` / `肉鸽` / `资源管理` / `挖矿` / `弹球` / `桌游` / `叙事` / `海龟汤` / `One Button`

**氛围/主题**：`治愈` / `微恐` / `心理悬疑`（不要 `噩梦`）/ `开放世界` / `欺骗机制`（不要 `博弈`）/ `二次元` / `第一人称`

**作品特征**：`Game Jam`（不要 `GGJ`，合并进来）/ `中传毕设` / `机核 BOOOM` / `获奖作品` / `英雄联盟同人`

**公司**：`搜狐畅游` / `字节跳动` / `腾讯光子`

**职位**：`游戏策划` / `战斗策划` / `实习` / `在研项目`

**禁用 tag**（已全部清理掉，不要再加）：
`学生作品`（冗余，作品集基本都是）、`2D`（太泛）、`C#`、`AI 对话`、`游戏设计`（太泛）、`博弈`、`噩梦`、`iOS`（平台细节，不做 tag）、`对话系统`（实现细节）、`主程`（是职责不是 tag）、`GGJ`、`2D 平台跳跃`

# 最近一次批量整理的成果（2026-04-22）

- 12 篇 portfolio 文章全部按上述结构对齐
- `bytedance-oasis.md` 和 `tencent-lightspeed.md` 因内容较少，只做了字段顺序对齐，未编造内容；后续如有可披露信息可按规范补充
